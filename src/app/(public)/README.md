# Public project routes

This route group reserves the presentation boundary for `/p/[slug]`.

Issue #14 will add the published project route. Until then, this group intentionally exposes no URL and contains no placeholder project data.
